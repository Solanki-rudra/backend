<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-950 text-white min-h-screen flex items-center justify-center px-4">

    <div class="bg-gray-900 p-8 rounded-xl w-full max-w-xl shadow-lg space-y-6">

        <h2 class="text-xl font-bold text-center">Edit Your Post</h2>

        <form action="/post/edit/<%= post._id %>" method="POST" class="space-y-4">
            <textarea name="content" required
                class="w-full h-40 bg-gray-800 text-white placeholder-gray-400 p-4 rounded-lg resize-none focus:outline-none focus:ring-2 focus:ring-indigo-500"><%= post.content %></textarea>

            <button type="submit"
                class="w-full py-2 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-lg transition">
                Save Changes
            </button>
        </form>

        <div class="text-center">
            <a href="/profile" class="text-sm text-gray-400 hover:text-gray-300 hover:underline">Cancel and go back</a>
        </div>

    </div>

</body>

</html>