<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dark Theme Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 min-h-screen p-10">
    <div class="flex flex-wrap gap-2">
        <% users.forEach(function(user){ %>
            <div class="bg-gray-800 text-white rounded-2xl shadow-lg p-6 max-w-sm w-full space-y-4">
                <div class="flex items-center space-x-4">
                    <img src='<%= user.image %>' alt="User Image"
                        class="w-20 h-20 rounded-full object-cover border-2 border-gray-600" />
                    <div>
                        <h3 class="text-xl font-semibold">
                            <%= user.name %>
                        </h3>
                        <p class="text-gray-400 text-sm">
                            <%= user.email %>
                        </p>
                    </div>
                </div>
                <div class="flex justify-end space-x-4 pt-2">
                    <a href="/edit/<%=user.id%>" class="text-blue-400 hover:text-blue-500 text-sm font-medium">Edit</a>
                    <a href="/delete/<%=user.id%>" class="text-red-400 hover:text-red-500 text-sm font-medium">Delete</a>
                </div>
            </div>
            <% }) %>
    </div>

</body>

</html>