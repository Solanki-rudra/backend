<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Backend</title>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center px-4">
    <div class="bg-zinc-800 p-8 rounded-lg shadow-lg max-w-xl w-full space-y-4 text-center">
        <a href="/" class="text-blue-600 underline">Back</a>

        <form method="post" action="/update/<%= filename %>" class="bg-zinc-800 p-6 rounded-lg shadow-md">
            <h2 class="text-2xl font-semibold mb-4">Edit section</h2>
            <div class="space-y-4">
                <input type="text" name="newTitle" placeholder="Enter title" value="<%= filename %>"
                    class="w-full p-3 bg-zinc-700 text-white rounded outline-none focus:ring-2 focus:ring-blue-500" />
                <textarea name="newDescription" placeholder="Enter description" rows="4"
                    class="w-full resize-none p-3 bg-zinc-700 text-white rounded outline-none focus:ring-2 focus:ring-blue-500"><%= data %></textarea>
                <button type="submit"
                    class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition duration-200">
                    Submit
                </button>
            </div>
        </form>

  </div>

</body>

</html>
